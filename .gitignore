# Secrets — never commit (plan §0 rule 8)
.dev.vars
.env
.env.*
*.local
.alchemy/

# Dependencies / build
node_modules/
dist/
.wrangler/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest/

# OS / editor
.DS_Store
*.log
*.bundle.mjs

# Vendored esbuild wasm (regenerate via npm run copy-wasm)
packages/mcp-server/src/sandbox/esbuild.wasm

# ServiceNow SDK build artifacts
sn-executor-app/fluent/node_modules/
sn-executor-app/fluent/dist/
sn-executor-app/fluent/target/
